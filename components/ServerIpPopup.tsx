import React, { useState } from "react";

function ServerIpPopup() {
	const [ip, setIp] = useState("");
	return <div className="flex flex-col gap-4 rounded-md px-4 py-2"></div>;
}

export default ServerIpPopup;
